# كيفية تشغيل السيرفر

## 🚀 الطرق السهلة لتشغيل السيرفر:

### الطريقة الأولى (الأسهل) - Double Click:
1. افتح مجلد `backend`
2. اضغط مرتين على ملف `start.bat` (لـ Windows)
3. أو اضغط مرتين على ملف `start.ps1` (لـ PowerShell)

### الطريقة الثانية - من Terminal:

#### في PowerShell:
```powershell
cd "d:\C4 Payments\backend"
.\start.ps1
```

#### في Command Prompt:
```cmd
cd "d:\C4 Payments\backend"
start.bat
```

### الطريقة الثالثة - مباشرة:
```powershell
cd "d:\C4 Payments\backend"
node src/server.js
```

### أو باستخدام npm:
```powershell
cd "d:\C4 Payments\backend"
npm start
```

## بعد تشغيل السيرفر:

✅ السيرفر هيشتغل على: **http://localhost:4000**

✅ افتح المتصفح وروح على:
- http://localhost:4000 (الصفحة الرئيسية - Login)
- http://localhost:4000/health (للتحقق من حالة السيرفر)

## ملاحظات:

1. تأكد إن الـ port 4000 مش مستخدم من برنامج تاني
2. لو في مشكلة، شوف الـ console output للأخطاء
3. السيرفر محمي من الأخطاء ومش هيقع لو حصل error

## بيانات الدخول الافتراضية:

- Email: `anas@c4.sa` أو `abdelhamid@c4.sa` أو `corecode@c4.sa`
- Password: `admin789`

